apiVersion: apps/v1
kind: Deployment
metadata:
  name: java-app-deployment
  namespace: bandera-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: java-app
  template:
    metadata:
      labels:
        app: java-app
    spec:
      containers:
        - name: java-app
          image: yabiskywalker/bandera-service-images:amd64
          ports:
            - containerPort: 8080
          env:
            - name: SPRING_DATA_MONGODB_URI
              value: "mongodb://root:password@mongodb-service:27017/jdbcStaticInfoDemo"
